import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.employeeManagement',
  appName: 'Employee Management',
  webDir: 'www'
};

export default config;
